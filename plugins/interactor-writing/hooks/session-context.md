Interactor Writing is installed.

- For ANY Interactor CMS request (list, search, show, open, edit, create, publish), first check which CMS connections this session has. The plugin's is `interactor-cms`; the writer may have added others such as `interactor-cms-personal`. If there is more than one, call `whoami` on each and note its account (email) and organization. Use the one that matches the request, or the default the folder's CLAUDE.md names; if neither decides it, ask the writer which "account · organization" they mean. Whenever you answer, say which account and organization it came from, and mention the other connections exist.
- To get, show, find, pull up, open, edit or work on a post or draft, or start a new one, use the `interactor-writing:write-post` skill: it opens the post as a formatted page beside the chat that the writer types on directly. Don't just describe the post in chat.
